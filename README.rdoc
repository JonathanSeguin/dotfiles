== NOTES

Must manually symlink .config/nvim otherwise .config will be overwritten on install (I need to fix this)


== Installation

  git clone git://github.com/JonathanSeguin/dotfiles.git ~/.dotfiles
  cd ~/.dotfiles
  rake install
